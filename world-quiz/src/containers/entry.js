import React from "react";
import {Container} from "@mui/material";

const Entry = () => {
    return (
        <Container maxWidth="md">
            <div>
                <h1>hello</h1>
            </div>
        </Container>
    );
};

export default Entry;
